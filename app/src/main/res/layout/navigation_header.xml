<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/primaryColor"
        app:navigationIcon="@drawable/action_bar_back_btn">

        <!--TextView
            android:id="@+id/tv_toolbar_title"
            style="@style/TextView.Toolbar.Title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Welcome"/-->

        <ImageButton
            android:id="@+id/btn_profiles"
            style="@style/Button.Navigation"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_drawer_account"
            android:adjustViewBounds="true"
            android:scaleType="centerInside"
            android:layout_gravity="center" />

        <ImageButton
            android:id="@+id/btn_show_likes"
            style="@style/Button.Navigation"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:scaleType="centerInside"
            android:layout_gravity="center"
            android:src="@drawable/ic_action_map_global"/>

        <ImageButton
            android:id="@+id/btn_show_profiles"
            style="@style/Button.Navigation"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:scaleType="centerInside"
            android:layout_gravity="center"
            android:src="@drawable/ic_action_image_photo_camera"/>

        <ImageButton
            android:id="@+id/btn_drawer_menu"
            style="@style/Button.Navigation"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_menu_list"
            android:layout_gravity="right" />
    </android.support.v7.widget.Toolbar>

</LinearLayout>